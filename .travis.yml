language: python

python:
    - "2.7"

before_install:
    - apt-get update -qq
    - apt-get install -qq libnotify
    - pip install requests

script:
    - nosetests

notifications:
    email: false
